/**
 * 旧 import path から新しい Cards 配下の display card field へつなぐ再 export です。
 *
 * Component 本体の責務は `Cards/DanceShortsDisplayCardField` に集約し、このファイルには表示ロジックを増やしません。
 */
export { default } from './Cards/DanceShortsDisplayCardField';
